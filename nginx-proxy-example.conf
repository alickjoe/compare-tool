# ============================================
# 文本对比工具 - Nginx 反向代理配置示例
# ============================================
# 
# 使用场景：在现有服务器上通过 /compare 路径访问
# 前提条件：
#   1. 已有 SSL 证书配置
#   2. Docker 容器运行在 3000 端口
#
# ============================================

# 在你的现有 server 块中添加以下 location 配置：

# HTTPS Server (443端口)
server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    # SSL 证书配置（根据你的实际情况修改）
    ssl_certificate /etc/nginx/ssl/your-domain.com.crt;
    ssl_certificate_key /etc/nginx/ssl/your-domain.com.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    
    # ... 你现有的其他配置 ...
    
    # ==========================================
    # 文本对比工具 - 添加以下配置
    # ==========================================
    
    location /compare/ {
        # 代理到 Docker 容器
        proxy_pass http://127.0.0.1:3000/;
        
        # 传递客户端真实信息
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持（如果需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # 静态资源缓存优化
    location ~* ^/compare/assets/.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # ... 你现有的其他 location 配置 ...
}

# ============================================
# 完整配置示例（独立 server 块）
# ============================================
# 如果你希望单独配置一个 server 块，可以使用以下完整配置：

# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#     
#     # SSL 配置
#     ssl_certificate /etc/nginx/ssl/your-domain.com.crt;
#     ssl_certificate_key /etc/nginx/ssl/your-domain.com.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     
#     # 安全头
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     
#     # 文本对比工具
#     location /compare/ {
#         proxy_pass http://127.0.0.1:3000/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
#     
#     # 其他网站内容
#     location / {
#         # 你现有的网站配置
#     }
# }

# ============================================
# HTTP 重定向到 HTTPS（可选）
# ============================================
# server {
#     listen 80;
#     server_name your-domain.com;
#     return 301 https://$server_name$request_uri;
# }
